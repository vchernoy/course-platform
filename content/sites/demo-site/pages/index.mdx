# Welcome

This is a **filesystem-backed demo site**. Public routes live under `/s/demo-site`.

![Demo banner](../assets/demo-banner.svg)

[Sample text file](../assets/sample.txt) — link rewrite uses the same `../assets/` convention as images.

Inline math example: $E = mc^2$.

:::note
Callouts work here via the same directive pipeline as lessons — without course-only MDX components.
:::
